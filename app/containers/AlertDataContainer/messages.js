/*
 * AlertDataContainer Messages
 *
 * This contains all the text for the AlertDataContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AlertDataContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AlertDataContainer container!',
  },
});
