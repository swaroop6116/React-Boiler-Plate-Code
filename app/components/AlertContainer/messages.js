/*
 * AlertContainer Messages
 *
 * This contains all the text for the AlertContainer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AlertContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AlertContainer component!',
  },
});
